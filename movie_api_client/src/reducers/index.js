
const initialState = {

}
const movieState = (state = initialState, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
            break;
    }
}

export default movieState;